name: 🚀 Deploy Next.js App via FTP

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🛒 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Install dependencies and build static site
        run: |
          npm install
          npm run build   # 👈 'out/' folder will now be generated automatically

      - name: 📤 Upload exported site to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 153.92.9.225
          username: u501537048.gitaction
          password: mJ27sqVxjCgZ=5>H
          local-dir: out/      # 👈 Next.js static output folder
          server-dir: /
          dangerous-clean-slate: false

